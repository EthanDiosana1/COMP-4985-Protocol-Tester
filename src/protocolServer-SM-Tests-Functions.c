#include "protocolServer-SM-Tests-Functions.h"
#include <stdlib.h>

int connect_correct_passcode(int argc, char *argv[])
{
    // Initialize the server manager.

    // Connect to the server and send the correct password.

    // Close the server manager.

    return EXIT_SUCCESS;
}

int connect_incorrect_passcode(int argc, char *argv[], int attempts)
{
    // Initialize the server manager.

    // Connect to the server and send an incorrect password password n times.

    // Close the server manager.

    return EXIT_SUCCESS;
}

int client_connect_normal(int argc, char *argv[])
{
    // Initialize the server manager.

    // Connect to the server and send the correct password.

    // Spawn a client and have it connect to the server.

    // Close the server manager.

    // Close the client.

    return EXIT_SUCCESS;
}

int client_connect_after_sending_q(int argc, char *argv[])
{
    // Initialize the server manager.

    // Connect to the server and send the correct password.

    // Send q to the server.

    // Spawn a client and have it connect to the server.

    // Close the server manager.

    // Close the client.
    return EXIT_SUCCESS;
}
