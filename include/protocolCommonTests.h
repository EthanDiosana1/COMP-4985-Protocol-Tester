#ifndef PROTOCOL_COMMON_TESTS_C
#define PROTOCOL_COMMON_TESTS_C

/*
 * @brief Runs the protocol tests that are common between client, server, and server manager.
 * @author Ethan D.
 */
int runCommonProtocolTests(void);

#endif
