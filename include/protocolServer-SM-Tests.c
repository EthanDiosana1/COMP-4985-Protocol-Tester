#ifndef PROTOCOL_SERVER_SM_TESTS_C
#define PROTOCOL_SERVER_SM_TESTS_C

#include "server_manager/serverManagerTools.h"

// sm connects to server using correct passcode
// sm connects to server using incorrect passcode
// sm connects to server using incorrect passcode 5 times
// client (use client spawner) connects to server before sm sends /q
// client (use client spawner) connects to server after sm sends /q
// send non-command message to server
// send malformed message to server

#endif
